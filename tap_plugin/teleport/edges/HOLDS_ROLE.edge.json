{
  "slug": "HOLDS_ROLE__teleport",
  "name": "Holds Role",
  "description": "A user or bot holds a role, and how: assigned statically, mapped at SSO login, granted by an access-list membership, or assumed through an approved access request.",
  "sources": [
    "teleport__teleport_user",
    "teleport__teleport_bot"
  ],
  "targets": [
    "teleport__teleport_role"
  ],
  "property_schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "granted_by": {
        "type": "string",
        "enum": [
          "static",
          "sso_mapping",
          "access_list",
          "access_request"
        ],
        "description": "Where the role came from — the question an access review asks."
      }
    }
  },
  "default_dimensions": {
    "teleport.plane": "policy"
  }
}
